{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "port": 9999,
            "name": "Debug backend in container ",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "localRoot": "${workspaceFolder}/backend",
            "remoteRoot": "/usr/src/app",
        }
    ]
}